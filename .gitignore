node_modules/
data/progress.json
# Written by POST /api/progress/import just before it overwrites the save file
data/progress.backup.json
.claude/settings.local.json

# TMDB API key — personal credential, never commit it
tmdb.key
